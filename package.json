{
  "name": "signbridge-deploy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "test": "npm --prefix backend test && npm --prefix frontend test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
